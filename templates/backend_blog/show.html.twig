<div class="modal-dialog modal-dialog-centered modal-lg" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h6 class="modal-title" id="exampleModalLabel4">{{ blog.titre }}</h6>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true"><i data-feather="x"></i></span>
            </button>
        </div>
        <div class="modal-body">
            <p class="mt-3">
                {% if blog.media %}
                    <img src="{{ absolute_url(asset('upload/blog/'~ blog.media)) }}" alt="{{ blog.titre }}" class="img-thumbnail" width="300" style="float: left; margin-right: 20px;">
                {% endif %}
                {{ blog.contenu|raw }}
            </p>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
            {{ include('backend_blog/_delete_form.html.twig') }}
            <a href="{{ path('backend_blog_edit', {'id': blog.id}) }}" class="btn btn-primary"> Modifier</a>
        </div>
    </div>
</div>