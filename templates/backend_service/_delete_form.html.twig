<form method="post" action="{{ path('backend_service_delete', {'id': service.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce service?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
